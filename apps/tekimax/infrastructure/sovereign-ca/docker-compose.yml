version: '3.8'

services:
  sovereign-ca:
    build: .
    container_name: sovereign-ca-service
    ports:
      - "11440:11440"
    volumes:
      - ./pki:/app/pki
    restart: unless-stopped
    networks:
      - sovereign-net

networks:
  sovereign-net:
    driver: bridge
